{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/scan-push-for-secrets.sh",
            "if": "Bash(git push*)",
            "timeout": 30,
            "statusMessage": "Scanning outgoing commits for secrets/PII before push..."
          }
        ]
      }
    ]
  }
}
